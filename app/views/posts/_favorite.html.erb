    <% if user_signed_in? && @post.favorited_by?(current_user)%>
  <%= link_to "お気に入り解除", post_favorite_path(@post), method: :delete, class: "btn btn-warning" %>
   <% else %>
  <%= link_to "お気に入り追加", post_favorites_path(@post), method: :post, class: "btn btn-primary" %>
    <% end %>